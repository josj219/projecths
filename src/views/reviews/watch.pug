extends ../layouts/base
include ../mixins/review


block content
    .reviews.container
            h1.review-title.heading-primary=review.movieName
            +review(review)

block scripts
    script(src="/static/js/reviewRating.js") 
    script(src="/static/js/main.js") 
