extends ../layouts/base
include ../mixins/photo


block content
    each potato in photoList
        +photo(potato)
    else
        li Sorry nothing found. 
    nav 
        ul
            li
                a(href="/gallery/upload") Upload Photo
